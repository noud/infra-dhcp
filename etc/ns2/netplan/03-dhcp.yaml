network:
    version: 2
    renderer: networkd
    ethernets:
        enp3s0:                               # Private network interface
            dhcp4: yes
            dhcp4-overrides:
                use-routes: false
                route-metric: 3
                # networkd
                use-dns: true
                use-hostname: false
